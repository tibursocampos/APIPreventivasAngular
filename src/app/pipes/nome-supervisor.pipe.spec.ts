import { NomeSupervisorPipe } from './nome-supervisor.pipe';

describe('NomeSupervisorPipe', () => {
  it('create an instance', () => {
    const pipe = new NomeSupervisorPipe();
    expect(pipe).toBeTruthy();
  });
});
