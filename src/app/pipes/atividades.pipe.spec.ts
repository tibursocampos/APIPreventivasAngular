import { AtividadesPipe } from './atividades.pipe';

describe('AtividadesPipe', () => {
  it('create an instance', () => {
    const pipe = new AtividadesPipe();
    expect(pipe).toBeTruthy();
  });
});
