import { EstadosBrPipe } from './estados-br.pipe';

describe('EstadosBrPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadosBrPipe();
    expect(pipe).toBeTruthy();
  });
});
