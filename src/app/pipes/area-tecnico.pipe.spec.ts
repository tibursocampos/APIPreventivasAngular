import { AreaTecnicoPipe } from './area-tecnico.pipe';

describe('AreaTecnicoPipe', () => {
  it('create an instance', () => {
    const pipe = new AreaTecnicoPipe();
    expect(pipe).toBeTruthy();
  });
});
